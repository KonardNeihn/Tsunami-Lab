##
# @author Alexander Breuer (alex.breuer AT uni-jena.de)
# @section DESCRIPTION
# Continuous integration using GitHub Actions.
##
name: Tsunami Lab

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: 0 0 * * *

jobs:
  CI:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          nix_path: nixpkgs=channel:nixos-24.05

      - name: Static Code Analysis (show Errors)
        run:
          nix-shell --run "
            cppcheck src/ --template=gcc --force
          "

      - name: Static Code Analysis (show Hints)
        run:
          nix-shell --run "
            cppcheck src/ --template=gcc --force --enable=all --error-exitcode=0
          "

      - name: Sanitize
        run: |
          nix-shell --run "
            scons mode=debug+san &&
            ./build/tests &&
            ./build/tsunami_lab 25 &&
            scons mode=release+san &&
            ./build/tests &&
            ./build/tsunami_lab 500
          "

      - name: Valgrind
        run: |
          nix-shell --run "
            scons mode=debug &&
            valgrind ./build/tests &&
            valgrind ./build/tsunami_lab 25
          "
          
      - name: Release
        run: |
          nix-shell --run "
            scons &&
            ./build/tests &&
            ./build/tsunami_lab 500
          "